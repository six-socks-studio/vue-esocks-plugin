mutation($lineItem: LineItemInput!, $id: ID!) {
    removeItemFromCart(
        id: $id,
        lineItem: $lineItem
    ) {
        id
        status
        totalPrice
        lineItems {
            price
            quantity
            sku
            productName
            variantNames {
                name
                presentation
            }
        }
    }
}
